server {
    listen 3000;
    root    /usr/share/nginx/html;
    index   index.html;
    etag on;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://forgeborn-api:8080;
    }
}